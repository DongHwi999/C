#include <stdio.h>
int main() {
	int a;
	int t; 
   for (int a='a'; i<='z'; i++) {
	   if (!(a=='a' || a=='e' || a=='i' || a=='o' || a=='u')) {
		   printf("%c", a);
	   }   
   }
   return 0;
}
